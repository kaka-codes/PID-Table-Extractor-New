# Copy this file to `.streamlit/secrets.toml` for local development.
# In Streamlit Community Cloud, paste the same key-value pair into
# the app's "Advanced settings" -> "Secrets" field during deployment.
GOOGLE_API_KEY = "replace-with-your-google-api-key"
